import { styled } from '@stitches/react';

export const ImageIcon = styled('img', {
  variants: {
    variant: {
      left: {
        marginLeft: '2%',
      },
      right: {
        marginRight: '2%',
      },
    },
  },
});
